# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins-dependencies
/build/
/coverage/
thirdparty/comaps

# iOS vendored frameworks (downloaded during pod install)
ios/Frameworks/

# iOS headers tarball (generated by bundle_ios_headers.sh)
ios/CoMaps-headers.tar.gz

# iOS downloaded headers for external consumers (downloaded during pod install)
ios/Headers/

# iOS headers staging directory (created during bundle_ios_headers.sh)
build/ios_headers_staging/

# macOS vendored frameworks (downloaded during pod install)
macos/Frameworks/

# macOS resources (copied during build)
macos/Resources/

# Android native libraries (downloaded during build)
android/src/main/jniLibs/

# Windows build artifacts
vcpkg_installed/

# Patch backup directories (created by regenerate_patches scripts)
patches_backup_*/

# Temporary native assets directory (created during Windows build)
example/build/native_assets/
