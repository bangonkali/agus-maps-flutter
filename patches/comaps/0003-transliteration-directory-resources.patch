diff --git a/libs/indexer/transliteration_loader.cpp b/libs/indexer/transliteration_loader.cpp
index 9ef60ab6..2ee0506c 100644
--- a/libs/indexer/transliteration_loader.cpp
+++ b/libs/indexer/transliteration_loader.cpp
@@ -17,19 +17,48 @@ void InitTransliterationInstanceWithDefaultDirs()
 
 #if defined(OMIM_OS_ANDROID)
   char const kICUDataFile[] = "icudt75l.dat";
-  if (!pl.IsFileExistsByFullPath(base::JoinPath(pl.WritableDir(), kICUDataFile)))
+  std::string const resourcesDir = pl.ResourcesDir();
+  std::string const writableDir = pl.WritableDir();
+  std::string const srcPath = base::JoinPath(resourcesDir, kICUDataFile);
+  std::string const dstPath = base::JoinPath(writableDir, kICUDataFile);
+  
+  // Check if the file already exists in writable dir
+  if (pl.IsFileExistsByFullPath(dstPath))
   {
+    LOG(LDEBUG, ("ICU data file already exists at:", dstPath));
+    Transliteration::Instance().Init(writableDir);
+    return;
+  }
+  
+  // If ResourcesDir is a directory (not a ZIP file), check if ICU file is there
+  if (!ZipFileReader::IsZip(resourcesDir))
+  {
+    if (pl.IsFileExistsByFullPath(srcPath))
+    {
+      LOG(LDEBUG, ("ICU data file found in resources directory:", srcPath));
+      // Use resources dir directly for transliteration
+      Transliteration::Instance().Init(resourcesDir);
+      return;
+    }
+    else
+    {
+      LOG(LWARNING, ("ICU data file not found in resources directory:", srcPath));
+    }
+  }
+  else
+  {
+    // ResourcesDir is a ZIP file (APK), extract the ICU data
     try
     {
-      ZipFileReader::UnzipFile(pl.ResourcesDir(), std::string("assets/") + kICUDataFile,
-                               pl.WritableDir() + kICUDataFile);
+      ZipFileReader::UnzipFile(resourcesDir, std::string("assets/") + kICUDataFile, dstPath);
+      LOG(LDEBUG, ("Extracted ICU data from ZIP to:", dstPath));
     }
     catch (RootException const & e)
     {
       LOG(LWARNING, ("Can't get transliteration data file \"", kICUDataFile, "\", reason:", e.Msg()));
     }
   }
-  Transliteration::Instance().Init(pl.WritableDir());
+  Transliteration::Instance().Init(writableDir);
 #else
   Transliteration::Instance().Init(pl.ResourcesDir());
 #endif
