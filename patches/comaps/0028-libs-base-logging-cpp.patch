diff --git a/libs/base/logging.cpp b/libs/base/logging.cpp
index 6b84f4b..9d2e286 100644
--- a/libs/base/logging.cpp
+++ b/libs/base/logging.cpp
@@ -31,7 +31,8 @@ std::optional<LogLevel> FromString(std::string const & s)
   ASSERT(!s.empty(), ("Log level should not be empty"));
 
   auto const & names = GetLogLevelNames();
-  auto const it = std::find(names.begin(), names.end(), std::toupper(s[0]));
+  // Cast to unsigned char to avoid assertion failure on Windows with non-ASCII characters
+  auto const it = std::find(names.begin(), names.end(), std::toupper(static_cast<unsigned char>(s[0])));
   if (it == names.end())
     return {};
   return static_cast<LogLevel>(std::distance(names.begin(), it));
